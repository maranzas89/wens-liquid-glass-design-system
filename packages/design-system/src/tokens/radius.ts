/**
 * Border radius tokens for the Liquid Glass Design System.
 */

export const radius = {
  /** Small controls (checkboxes, tags) */
  sm: '0.5rem',      // 8px
  /** Default controls (buttons, inputs) */
  md: '0.75rem',     // 12px
  /** Cards, modals, larger surfaces */
  lg: '1rem',        // 16px
  /** Primary card/panel radius */
  xl: '1.5rem',      // 24px
  /** Full-round (pills, avatars) */
  full: '9999px',
} as const;
